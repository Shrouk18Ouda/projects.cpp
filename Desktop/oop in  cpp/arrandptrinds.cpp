#include<iostream>
using namespace std;
int main()
{
    int a[5],i;
    int *q;
    q=a;
    for(i=0;i<5;i++)
    {
    //cin>>a[i];// or q[i]
    //cin>>*(a+i);// or (q+i) or (i+a) or (i+q)
    cin>>i[q];//or i[a]
    }
    for(i=0;i<5;i++)
    {
       // cout<<a[i]; // or q[i]
       // cout<<*(q+i);// or *(a+i) or *(i+a) or *(i+q)
        cout<<i[a];//or i[q]
    }   
}
